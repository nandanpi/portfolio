---
import { techStack } from "../lib/constants";
---

<section class="py-20 px-6 bg-slate-800/50">
    <div class="max-w-7xl mx-auto">
        <div class="grid lg:grid-cols-12 gap-8">
            <div class="lg:col-span-4">
                <h2 class="text-3xl font-bold text-white mb-6">
                    Technologies I work with
                </h2>
                <p class="text-slate-300 leading-relaxed">
                    Here are some of the technologies I'm passionate about and
                    use regularly in my projects.
                </p>
            </div>

            <div class="lg:col-span-8">
                <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                    {
                        techStack.map((tech) => (
                            <div
                                class={`p-4 rounded-2xl border-2 ${tech.color} hover:scale-105 transition-transform duration-200 backdrop-blur-sm`}
                            >
                                <div class="font-semibold">{tech.name}</div>
                                <div class="text-xs opacity-70 mt-1">
                                    {tech.category}
                                </div>
                            </div>
                        ))
                    }
                </div>
            </div>
        </div>
    </div>
</section>
